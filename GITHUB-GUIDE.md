# راهنمای بیلد گرفتن روی GitHub Actions (بدون نیاز به اندروید استودیو)

## هشدار امنیتی مهم

**کلید (Token) گیتهاب خودت را به هیچ‌کس نده — نه حتی به من (دستیار هوش مصنوعی).**

توکن گیتهاب مثل رمز عبور کامل اکانت شماست؛ هر کسی آن را داشته باشد می‌تواند
ریپوهایتان را پاک کند، کد بنویسد یا اکانت را بگیرد. هیچ نیازی به فرستادن توکن
در چت نیست، چون GitHub Actions **به‌صورت خودکار** بعد از آپلود کد، بیلد را اجرا می‌کند.

---

## روش ۱: بدون هیچ کلیدی (ساده‌ترین راه — فقط با مرورگر) ⭐

### قدم ۱: ساخت اکانت و ریپو
1. برو به `github.com` و ثبت‌نام کن (یا وارد شو).
2. روی علامت **+** بالای صفحه بزن و **New repository** را انتخاب کن.
3. اسم بگذار `NeuAnki`، گزینه **Public** را انتخاب کن (بیلد رایگان است) و **Create repository** بزن.

### قدم ۲: آپلود پروژه
1. در صفحه ریپو، روی لینک **«uploading an existing file»** کلیک کن
   (یا از دکمه **Add file ← Upload files**).
2. **محتویات پوشه `NeuAnki`** (همه فایل‌ها و پوشه‌های داخلش) را با موس بکش و
   داخل کادر آپلود رها کن. مرورگر خودش ساختار پوشه‌ها را حفظ می‌کند.
   - نکته: خودِ پوشه `NeuAnki` را نکش؛ چیزهای *داخل* آن را بکش تا ساختار درست بماند.
   - مطمئن شو پوشه `.github` هم آپلود شده (در ویندوز پوشه‌های نقطه‌دار مخفی نیستند ولی حواست باشد حذفش نکنی).
3. پایین صفحه روی **Commit changes** بزن و صبر کن آپلود تمام شود.

### قدم ۳: دانلود فایل APK
1. بالای ریپو برو به تب **Actions**.
2. یک بیلد با اسم **«Build NeuAnki APK»** در حال اجرا می‌بینی (زمان تقریبی: ۵ تا ۸ دقیقه).
3. بعد از اینکه دایره‌اش **سبز** شد، روی آن کلیک کن.
4. پایین صفحه در بخش **Artifacts** دو فایل می‌بینی:
   - `NeuAnki-debug-apk` → نصب‌کن مستقیم روی گوشی
   - `NeuAnki-release-apk` → خروجی نهایی (امضاشده و قابل نصب)
5. روی آن کلیک کن تا **zip** دانلود شود؛ داخلش فایل `app-debug.apk` هست.
   فایل را به گوشی بفرست و نصب کن (Allow from unknown sources).

> هر بار که کد را تغییر دهی و دوباره آپلود کنی، بیلد جدید به‌صورت خودکار اجرا می‌شود.
> اگر خواستی بیلد را دستی اجرا کنی: تب Actions ← Build NeuAnki APK ← Run workflow.

---

## روش ۲: با Git و توکن شخصی (برای کاربران حرفه‌ای‌تر)

توکن فقط برای **خودت** لازم است تا با خط فرمان push کنی. آن را در چت یا جایی دیگر به اشتراک نگذار.

### ساخت توکن (فقط برای خودت)
1. گیتهاب ← بالای راست روی عکس پروفایل ← **Settings**
2. پایین صفحه ← **Developer settings**
3. **Personal access tokens ← Tokens (classic)**
4. **Generate new token (classic)** ← فقط تیک **repo** را بزن
5. **Generate token** ← دکمه سبز
6. توکن (`ghp_....`) را کپی کن — **فقط یک بار نمایش داده می‌شود!**

### ارسال پروژه
بعد از ساخت ریپوی خالی در سایت، در کامپیوتر خودت داخل پوشه `NeuAnki`:

```bash
git init
git add .
git commit -m "NeuAnki: initial release"
git branch -M main
git remote add origin https://github.com/USERNAME/NeuAnki.git
git push -u origin main
```

وقتی رمز خواست:
- **Username:** یوزرنیم گیتهابت
- **Password:** همانی توکن را بچسبان (نه رمز اکانت!)

بعدش برو تب Actions و APK را مثل روش ۱ دانلود کن.

### انتشار نسخه با تگ (اختیاری)
هر وقت خواستی نسخه رسمی منتشر کنی:

```bash
git tag v1.0
git push origin v1.0
```

این کار علاوه بر Artifacts، یک **Release** هم در تب Releases می‌سازد و هر دو APK را
به آن می‌چسباند (برای دانلود بدون لاگین در ریپوی Public).

---

## مشکلات رایج

| مشکل | راه‌حل |
|---|---|
| تب Actions وجود ندارد | ریپو را Public بساز؛ در ریپوی Private هم Actions فعال است ولی گاهی باید در Settings ← Actions اجازه بدهی |
| بیلد قرمز شد | روی بیلد کلیک کن ← مرحله قرمز را باز کن ← متن خطا را بردار |
| APK نصب نمی‌شود | در تنظیمات گوشی: Install unknown apps را برای مرورگر/تلگرام فعال کن |
| می‌خواهم هر دو APK را نصب کنم | ممکن است چون applicationId یکی است تداخل کنند؛ یکی را نصب کن |
